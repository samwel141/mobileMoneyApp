import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    textColor: {
        color: 'black',
    },
    textFontFamily: {
        fontFamily: 'KumbhSans-VariableFont_YOPQ,wght'
        //Platform.OS === 'android' ? 'Onest-Regular' : 'Onest-Regular',
    }
});

export default styles;
